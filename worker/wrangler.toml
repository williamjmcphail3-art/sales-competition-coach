# Cloudflare Worker config. Deploy from this folder with:  npx wrangler deploy
#
# Before first deploy, set your Groq key as a secret (never committed):
#   npx wrangler secret put GROQ_API_KEY     (get a free key at https://console.groq.com/keys)
#
# Optional: lock the Worker to your site's origin instead of allowing any page.
# Set ALLOWED_ORIGIN to your GitHub Pages URL (no trailing slash), e.g.
#   [vars]
#   ALLOWED_ORIGIN = "https://YOUR-USER.github.io"

name = "sales-competition-coach"
main = "worker.js"
compatibility_date = "2024-11-01"

# [vars]
# ALLOWED_ORIGIN = "*"
